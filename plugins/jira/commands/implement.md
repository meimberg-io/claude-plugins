---
description: Implement a Jira ticket — read spec, assess complexity, transition through the workflow, commit per ticket.
---

Run the **implement** skill.

Arguments: `$ARGUMENTS`

Resolve the issue key from the arguments (full key like `MIVOLVE-42`, or just `42` which gets prefixed with the configured project key). Read the spec, assess whether to implement directly or enter Plan mode, transition the ticket through the workflow, implement, then commit one focused commit for this ticket. Do not push.
